/**
 * Submits the resume details to the backend for PDF generation
 * @returns true if successful; false otherwise
 */
export default function SubmitResumeDetails (details) {
    return true; // all was successful
}